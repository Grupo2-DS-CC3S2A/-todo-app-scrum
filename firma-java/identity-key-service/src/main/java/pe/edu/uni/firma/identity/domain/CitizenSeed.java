package pe.edu.uni.firma.identity.domain;

/** Datos base generados para poblar simultáneamente la BD privada y la BD pública. */
public record CitizenSeed(String dni, String apellidosNombres, String ubigeo) {}
